# Default values for helmstreet-values.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

helmstreet:  
  replicaCount: 1

helmstreet2:
  replicaCount: 2
  namespacesOverride: helmstreet-incubo